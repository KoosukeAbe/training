<head>
  <meta charset="utf-8">
  <link rel="stylesheet" href="task.scss" type="text/scss">
</head>
<body>
  <h1><%= t '.title' %></h1>
    
  <% @tasks.each do |task| %>
    <%= link_to "#{t '.task_title'}：#{task.title}", task_path(task) %>
    <br>
  <% end %>
  <%= button_to (t '.create_button'), new_task_path, method: :get %>
</body>